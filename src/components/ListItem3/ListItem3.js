import React, { useState } from "react";
// import styles from "./ListItem.css";

const ListItem3 = ( {dataProperty} ) =>{
  // let [data,setData] = useState('Empty');
  // const dataPropertyHandler = (dataProp) => {
  //   setData(dataProp);
  // }
  return (
       <span>{dataProperty}</span> 
  );
};

export default ListItem3;
